import "./styles/styles.css";
import { initCalculator } from "./components/initCalculator.js";
document.addEventListener("DOMContentLoaded", () => {
    initCalculator();
    // initCalculator();
});
